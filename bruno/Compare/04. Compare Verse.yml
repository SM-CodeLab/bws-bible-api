info:
  name: 04. Compare Verse
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/compare/{{idBible}}/{{idBibleToCompare}}/{{idBook}}/{{idChapter}}:1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
