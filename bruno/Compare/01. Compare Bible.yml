info:
  name: 01. Compare Bible
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/compare/{{idBible}}/{{idBibleToCompare}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
