info:
  name: 02. Compare Book
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/compare/{{idBible}}/{{idBibleToCompare}}/{{idBook}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
