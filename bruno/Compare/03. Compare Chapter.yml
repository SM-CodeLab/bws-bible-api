info:
  name: 03. Compare Chapter
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/compare/{{idBible}}/{{idBibleToCompare}}/{{idBook}}/{{idChapter}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
