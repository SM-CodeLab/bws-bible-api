info:
  name: 02. Explore Book
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/explore/{{idBible}}/{{idBook}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
