info:
  name: 04. Explore Verse
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/explore/{{idBible}}/{{idBook}}/{{idChapter}}:1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
