info:
  name: 01. Explore Bible
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiUrl}}/explore/{{idBible}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
