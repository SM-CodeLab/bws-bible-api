info:
  name: 04. Search Verse
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiUrl}}/search/{{idBible}}/{{idBook}}/{{idChapter}}:1/{{searchWords}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
