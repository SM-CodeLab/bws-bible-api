info:
  name: 02. Search Book
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}/search/{{idBible}}/{{idBook}}/{{searchWords}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
