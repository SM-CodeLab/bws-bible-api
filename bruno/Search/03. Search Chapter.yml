info:
  name: 03. Search Chapter
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiUrl}}/search/{{idBible}}/{{idBook}}/{{idChapter}}/{{searchWords}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
